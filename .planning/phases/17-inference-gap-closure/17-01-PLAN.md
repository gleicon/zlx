---
phase: 17-inference-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.zig
  - src/config.zig
  - src/inference/mod.zig
  - src/inference/generator.zig
  - src/api/streaming.zig
autonomous: true
requirements: [GAP-07]
must_haves:
  truths:
    - "zig build succeeds with no undefined references to speculation symbols"
    - "No --draft-model, --no-speculation, --speculation-depth flags exist in CLI help output"
    - "src/speculation/ directory is deleted and not imported from any file"
  artifacts:
    - path: "src/main.zig"
      provides: "CLI arg parsing without speculation flags"
      contains: "// speculative-decoding: removed"
    - path: "src/config.zig"
      provides: "AppConfig without draft_model/speculation fields"
      contains: "// speculative-decoding: removed"
    - path: "src/inference/mod.zig"
      provides: "generateWithTimeout without null draft_model args"
      contains: "// speculative-decoding: removed"
  key_links:
    - from: "src/main.zig"
      to: "src/speculation/mod.zig"
      via: "import"
      pattern: "speculation"
      note: "Import and all symbol uses must be removed"
---

<objective>
Remove all speculative decoding code from the codebase. Zero dead code policy (D-03).

Purpose: The speculation subsystem was scaffolded but never produced real output. Keeping dead code raises complexity without benefit and makes future audits harder. The user explicitly chose deletion over documentation.
Output: A clean codebase with no speculation symbols, verified by a passing zig build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-inference-gap-closure/17-CONTEXT.md

Decision D-03: Remove all speculative decoding code — zero dead code policy. Add comment at each
removal site: `// speculative-decoding: removed — re-evaluate as dedicated phase after core inference is stable`
</context>

<interfaces>
<!-- Speculation removal inventory from 17-RESEARCH.md — exact lines to delete. -->

src/main.zig:
  - Line 14: `const speculation = @import("speculation/mod.zig");`  → delete
  - Lines 42-44: USAGE string lines for --draft-model, --speculation-depth, --no-speculation  → delete
  - Line 63: USAGE example with --draft-model  → delete
  - Lines 100-102: draft_model, speculation_depth, no_speculation in Config struct  → delete 3 fields
  - Lines 251-256: --draft-model CLI parse block  → delete block
  - Lines 257-265: --speculation-depth CLI parse block  → delete block
  - Lines 267-268: --no-speculation CLI parse block  → delete block
  - Lines 330-339: Log block for speculation settings  → delete block
  - Lines 358-360: convertFileConfig() speculation field assignments  → delete 3 lines
  - Lines 559-588: Entire speculation init block (if/else, configure, defer shutdown)
      → replace with: `defer manager_mod.deinitGlobalManager(allocator);`

src/config.zig:
  - Line 34: draft_model: ?[]const u8 = null  → delete
  - Line 35: speculation_depth: usize = 4  → delete
  - Line 36: no_speculation: bool = false  → delete
  - Lines 53-55: same 3 fields in FileConfig  → delete
  - Lines 165-167: field copy assignments in merge function  → delete 3 lines
  - Line 252: result.draft_model = ... env var assignment  → delete
  - Lines 257-261: ZLX_SPECULATION_DEPTH env var block  → delete
  - Lines 268-270: ZLX_NO_SPECULATION env var block  → delete
  - Lines 316-318: speculation_depth validation block  → delete

src/inference/mod.zig:
  - Lines 244-261: .deepseek_v2_moe match arm in generateWithTimeout() switch  → delete entire arm
  - Lines 280-281: `null, // draft_model` and `0, // speculation_depth` args  → remove these 2 args
  - Lines 405-406: same `null, // draft_model` and `0, // speculation_depth` args in generateWithLogprobs()  → remove these 2 args
  - Line 332: `return error.DeepSeekNotImplemented;` after switch  → delete (unreachable)
  Note: also remove those 2 params from GenerationState.init() signature in generator.zig

src/inference/generator.zig:
  - GenerationState struct fields: remove `draft_model` and `speculative_generator` / `use_speculation` fields
  - GenerationState.init() signature: remove `draft_model_ref` and `speculation_depth` params
  - Lines 181-213: entire speculation initialization block in init()  → delete
  - Any speculation branch in next() and generateAll()  → delete

src/api/streaming.zig:
  - Lines 141-142: `null, // draft_model` and `0, // speculation_depth` args to GenerationState.init()  → remove
  - Lines 410-411: same args in second GenerationState.init() call  → remove
  - Any other reference to speculation metrics or draft token tracking  → remove

NOTE: All four GenerationState.init() call sites must be updated atomically:
  1. inference/mod.zig:280-281
  2. inference/mod.zig:405-406
  3. api/streaming.zig:141-142
  4. api/streaming.zig:410-411
If any call site still passes the old arg count, zig build will fail with a mismatch error.

After file edits: `rm -rf src/speculation/` to delete the directory.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove speculation fields and imports from main.zig and config.zig</name>
  <files>src/main.zig, src/config.zig</files>
  <action>
Edit src/main.zig:
- Delete line 14: the `@import("speculation/mod.zig")` import
- Delete the 3 CLI flag blocks for --draft-model, --speculation-depth, --no-speculation (lines ~251-268)
- Delete the 3 Config struct fields: draft_model, speculation_depth, no_speculation (lines ~100-102)
- Delete the USAGE string lines mentioning those flags (lines ~42-44 and ~63)
- Delete the log block for speculation settings in parseArgs() (lines ~330-339)
- Delete the 3 convertFileConfig() field assignments (lines ~358-360)
- Replace the speculation init block (lines ~559-588) with a single comment:
  `// speculative-decoding: removed — re-evaluate as dedicated phase after core inference is stable`
  followed by `defer manager_mod.deinitGlobalManager(allocator);` if that line was inside the block

Edit src/config.zig:
- Delete draft_model, speculation_depth, no_speculation from AppConfig (lines ~34-36)
- Delete same 3 fields from FileConfig (lines ~53-55)
- Delete the 3 merge() field assignments (lines ~165-167)
- Delete the draft_model env var line (~252), ZLX_SPECULATION_DEPTH block (~257-261), ZLX_NO_SPECULATION block (~268-270)
- Delete speculation_depth validation block (~316-318)
- Add `// speculative-decoding: removed — re-evaluate as dedicated phase after core inference is stable` at each removal site

After each deletion verify the surrounding code is syntactically valid (closing braces, trailing commas in structs, etc.).
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build 2>&1 | grep -i "speculation" | head -20</automated>
  </verify>
  <done>No compilation errors referencing speculation symbols from main.zig or config.zig. "speculation" does not appear in zig build output.</done>
</task>

<task type="auto">
  <name>Task 2: Remove speculation from inference/mod.zig, generator.zig, streaming.zig, and delete speculation/ directory</name>
  <files>src/inference/mod.zig, src/inference/generator.zig, src/api/streaming.zig</files>
  <action>
Edit src/inference/mod.zig:
- Delete the entire `.deepseek_v2_moe` match arm in generateWithTimeout() (lines ~244-261)
- Remove the 2 null speculation args from the GenerationState.init() call in generateWithTimeout() (lines ~280-281):
  remove `null, // draft_model` and `0, // speculation_depth` positional args
- Remove the same 2 null speculation args from the GenerationState.init() call in generateWithLogprobs() (lines ~405-406)
- Delete the now-unreachable `return error.DeepSeekNotImplemented;` line (~332)
- Add removal comment at each site

Edit src/inference/generator.zig:
- Find GenerationState struct definition — remove `draft_model`, `speculative_generator`, `use_speculation` fields
- Find GenerationState.init() function signature — remove `draft_model_ref` and `speculation_depth` params
- Delete the entire speculation initialization block in init() (~lines 181-213)
- Remove speculation branches from next() and generateAll()
- Add removal comments at each site

Edit src/api/streaming.zig:
- Find GenerationState.init() calls at lines ~141-142 and ~410-411
- Remove the `null, // draft_model` and `0, // speculation_depth` args from both call sites
- Search for any other speculation-related fields (draft token count, speculative metrics)
- Remove any such references; add removal comments

CRITICAL: All four GenerationState.init() call sites must be updated in the same task.
Run `zig build` after all four sites are patched — not between individual file edits.

Then delete the speculation directory:
  `rm -rf /Users/gleicon/code/zig/zlx/src/speculation/`

Run `zig build` to confirm a clean build. Fix any cascading symbol errors from the parameter removals.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build 2>&1 | head -30 && echo "---" && ls src/speculation 2>&1</automated>
  </verify>
  <done>zig build succeeds without errors. `ls src/speculation` returns "No such file or directory". No "NotImplemented" or "speculation" references remain in the inference pipeline files.</done>
</task>

</tasks>

<verification>
After both tasks:
- `zig build` produces no errors
- `grep -r "speculation" src/ --include="*.zig"` returns only the removal comments (`// speculative-decoding: removed`)
- `grep -r "draft_model\|no_speculation\|speculation_depth" src/ --include="*.zig"` returns nothing
- `ls src/speculation` returns "No such file or directory"
</verification>

<success_criteria>
All speculation symbols removed. `zig build` passes. No live code references `draft_model`, `no_speculation`, `speculation_depth`, or `src/speculation/`. Removal comment present at each deletion site.
</success_criteria>

<output>
After completion, create `.planning/phases/17-inference-gap-closure/17-01-SUMMARY.md` with:
- Files modified
- Symbols removed (with line ranges)
- Confirmation that zig build passes
- Any unexpected compilation issues encountered
</output>
