---
phase: 16-build-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mlx.zig/build.zig
  - build.zig
autonomous: true
requirements: [GAP-08]

must_haves:
  truths:
    - "`zig build` reaches the compilation stage (no build-script errors)"
    - "No `b.pathJoin` deprecation errors or panics abort the build step"
    - "All `b.addSystemCommand` path arguments remain plain `[]const u8` (they already are — no change needed)"
  artifacts:
    - path: "src/mlx.zig/build.zig"
      provides: "Submodule build script, fixed in-place (D-01)"
      contains: "setupDependencies"
    - path: "build.zig"
      provides: "Top-level build script with all pathJoin calls audited"
      contains: "setupDependencies"
  key_links:
    - from: "build.zig"
      to: "src/mlx.zig/build.zig"
      via: "configureExecutable call"
      pattern: "configureExecutable"
    - from: "src/mlx.zig/build.zig"
      to: "mlx-c static library"
      via: "addObjectFile(.{ .cwd_relative = b.pathJoin(...) })"
      pattern: "addObjectFile"
---

<objective>
Fix all Zig 0.15.2 build-system deprecation errors in `src/mlx.zig/build.zig` and `build.zig` so that `zig build` runs without aborting on pathJoin or LazyPath mismatches.

Purpose: GAP-08. Zig 0.15.2 changed how `b.pathJoin` interacts with `LazyPath`-expecting APIs. Several call sites already use `.{ .cwd_relative = b.pathJoin(...) }` which is correct. The audit must confirm every call site follows this pattern — and fix any that pass a raw `[]const u8` where a `LazyPath` is required.

Output: Both build scripts pass the Zig build-system evaluation phase without errors or compilation-aborting deprecations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-build-gap-closure/16-CONTEXT.md

<interfaces>
<!-- Key patterns already in use — follow these, do NOT invent new ones. -->

From src/mlx.zig/build.zig (CORRECT pattern — already present):
```zig
// pathJoin returns []const u8 — wrap with .cwd_relative for LazyPath slots
exe.addIncludePath(.{ .cwd_relative = deps.mlx_c_path });
exe.addObjectFile(.{ .cwd_relative = b.pathJoin(&.{ deps.mlx_c_build_path, "libmlxc.a" }) });
cmake_cmd.setCwd(.{ .cwd_relative = mlx_c_build_path });
```

From build.zig (CORRECT pattern — already in use):
```zig
exe.addIncludePath(.{ .cwd_relative = b.pathJoin(&.{ llama_cpp_path, "include" }) });
exe.addLibraryPath(.{ .cwd_relative = b.pathJoin(&.{ llama_build_path, "src" }) });
```

From build.zig (addSystemCommand — pathJoin result passed directly as []const u8, which IS correct):
```zig
const mkdir_cmd = b.addSystemCommand(&.{ "mkdir", "-p", dest_dir });
const copy_cmd = b.addSystemCommand(&.{ "cp", src_path, dest_path });
// addSystemCommand takes []const []const u8 — plain strings are correct here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit src/mlx.zig/build.zig for pathJoin call sites</name>
  <files>src/mlx.zig/build.zig</files>
  <action>
Read `src/mlx.zig/build.zig` in full. For each `b.pathJoin(...)` call, classify whether the result is:
  (a) Passed to a `LazyPath`-expecting API (addIncludePath, addObjectFile, addLibraryPath, setCwd, root_source_file) — MUST use `.{ .cwd_relative = b.pathJoin(...) }` wrapper
  (b) Passed to `addSystemCommand` as a plain string argument — plain `[]const u8` is CORRECT, no change needed
  (c) Stored in a local variable and later used — fix the usage site, not the storage

Fix any (a) sites that are missing the `.{ .cwd_relative = ... }` wrapper. Per D-01, edit this file in-place.

Known correct patterns already in file (DO NOT change):
  - `exe.addIncludePath(.{ .cwd_relative = deps.mlx_c_path })` — already correct
  - `exe.addObjectFile(.{ .cwd_relative = b.pathJoin(&.{ deps.mlx_c_build_path, "libmlxc.a" }) })` — already correct
  - `cmake_cmd.setCwd(.{ .cwd_relative = mlx_c_build_path })` — already correct
  - `doesFileExist(b.pathJoin(...))` — `doesFileExist` takes `[]const u8`, correct
  - `addSystemCommand(&.{ ..., b.pathJoin(...), ... })` — takes `[]const u8`, correct

If all call sites are already correct (the file may already be fixed), make no changes and note this in the task output.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build 2>&1 | head -30</automated>
  </verify>
  <done>No pathJoin-related errors in `zig build` output from src/mlx.zig/build.zig</done>
</task>

<task type="auto">
  <name>Task 2: Audit top-level build.zig for pathJoin call sites</name>
  <files>build.zig</files>
  <action>
Read `build.zig` in full. Apply the same classification as Task 1.

Known call sites to check (from grep output):
  - Line 80: `const llama_build_path = b.pathJoin(&.{ llama_cpp_path, "build" })` — stored as `[]const u8`, used later
  - Line 113: `exe.addIncludePath(.{ .cwd_relative = b.pathJoin(...) })` — already correct
  - Line 114: `exe.addIncludePath(.{ .cwd_relative = b.pathJoin(...) })` — already correct
  - Line 117: `exe.addLibraryPath(.{ .cwd_relative = b.pathJoin(...) })` — already correct
  - Line 299: `b.pathJoin(&.{ "/Users/gleicon/...", deps.mlx_c_v4_path })` — check usage
  - Lines 638-645 (setupDependencies): variables storing pathJoin results — check each usage site
  - Lines 716-722: `doesFileExist(b.pathJoin(...))`, `addSystemCommand` paths — both take `[]const u8`, correct

For each stored variable (like `llama_build_path`), trace all usage sites. If any usage passes it directly to a `LazyPath`-expecting slot without `.{ .cwd_relative = ... }`, add the wrapper at the usage site.

Note: `b.addSystemCommand(&.{..., path, ...})` — `addSystemCommand` takes `[]const []const u8`. Plain strings here are CORRECT. Do not wrap those.

After making any fixes, verify the build runs without build-script errors.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build 2>&1 | grep -E "error:|deprecated|b\.pathJoin" | head -20</automated>
  </verify>
  <done>`zig build` produces no pathJoin deprecation errors or build-script panics. Build may still fail on source compilation errors (GAP-09, GAP-10) — that is expected and acceptable here.</done>
</task>

</tasks>

<verification>
Run `zig build 2>&1 | head -50`. The output should show compilation errors (undefined symbols, struct mismatches) — NOT build-script evaluation errors. Build-script errors appear before any "Compiling..." lines. If you see `error: use of undefined identifier 'pathJoin'` or `error: expected type 'std.Build.LazyPath'` — those are build-script errors that must be fixed. If you see `error: undeclared identifier 'arrayIsEmpty'` or `error: no field named 'weights'` — those are source errors fixed in plans 02 and 03, not in scope here.
</verification>

<success_criteria>
`zig build` reaches source compilation (emits at least one `Compiling src/...` line or a linker error) without aborting on build-script evaluation. All `b.pathJoin` call sites in both build files follow the correct pattern for their context.
</success_criteria>

<output>
After completion, create `.planning/phases/16-build-gap-closure/16-01-SUMMARY.md` with:
- What was found (already-correct call sites vs. sites that needed fixing)
- Files changed and line numbers
- Result of `zig build 2>&1 | head -30`
</output>
