---
phase: 16-build-gap-closure
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/mlx.zig/src/mlx.zig
autonomous: true
requirements: [GAP-09]

must_haves:
  truths:
    - "`mlx.arrayIsEmpty(arr)` compiles without undefined symbol errors"
    - "`zig build test` does not fail with a linker error for `arrayIsEmpty`"
    - "All existing test assertions in `src/test_integration.zig` and `src/inference/dequantize.zig` that call `mlx.arrayIsEmpty()` survive unchanged"
  artifacts:
    - path: "src/mlx.zig/src/mlx.zig"
      provides: "MLX bindings layer — now includes arrayIsEmpty wrapper"
      exports: ["arrayIsEmpty"]
      contains: "pub fn arrayIsEmpty"
  key_links:
    - from: "src/test_integration.zig"
      to: "src/mlx.zig/src/mlx.zig"
      via: "mlx.arrayIsEmpty(arr) call"
      pattern: "arrayIsEmpty"
    - from: "src/inference/dequantize.zig"
      to: "src/mlx.zig/src/mlx.zig"
      via: "mlx.arrayIsEmpty(arr) call"
      pattern: "arrayIsEmpty"
    - from: "src/mlx.zig/src/mlx.zig"
      to: "mlx-c C API"
      via: "C.mlx_array_size(arr)"
      pattern: "mlx_array_size"
---

<objective>
Add `pub fn arrayIsEmpty(arr: Array) bool` to `src/mlx.zig/src/mlx.zig`, wrapping `C.mlx_array_size(arr) == 0`. This resolves the undefined symbol at link time for all call sites in `src/test_integration.zig` (~10 calls) and `src/inference/dequantize.zig` (~3 calls).

Purpose: GAP-09. `mlx.arrayIsEmpty` is called throughout the codebase but was never defined in the MLX bindings layer. The fix is a one-function addition. Per D-02, do NOT remove any test assertions.

Output: `src/mlx.zig/src/mlx.zig` with one new exported function. No other files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-build-gap-closure/16-CONTEXT.md
@.planning/phases/16-build-gap-closure/16-01-SUMMARY.md

<interfaces>
<!-- Existing wrapper pattern to follow — place arrayIsEmpty adjacent to arrayFree -->

From src/mlx.zig/src/mlx.zig (existing wrappers, lines ~108-216):
```zig
pub const arrayShape = C.mlx_array_shape;   // line 108 — direct alias

pub fn arrayFree(arr: Array) void {          // line 214 — void wrapper
    _ = C.mlx_array_free(arr);
}
```

C function available in mlx-c v0.1.2 (confirmed present in mlx.h):
```c
// Returns total number of elements in the array (0 if empty/uninitialized)
size_t mlx_array_size(mlx_array a);
```

New function to add (place after arrayFree, before the Vector Operations section):
```zig
pub fn arrayIsEmpty(arr: Array) bool {
    return C.mlx_array_size(arr) == 0;
}
```

Call sites that MUST NOT be changed (per D-02):
  src/test_integration.zig lines 51-53, 60-61, 161-163, 170-172, 177-179
  src/inference/dequantize.zig lines 92-94, 243-245
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add arrayIsEmpty wrapper to mlx.zig bindings</name>
  <files>src/mlx.zig/src/mlx.zig</files>
  <action>
Read `src/mlx.zig/src/mlx.zig`. Find the `arrayFree` function (around line 214). Immediately after it, add the new function:

```zig
pub fn arrayIsEmpty(arr: Array) bool {
    return C.mlx_array_size(arr) == 0;
}
```

Placement requirement: it must be in the `/// Array Operations` section, adjacent to `arrayFree` and `arrayShape`. Do not add it at the top or bottom of the file — keep it co-located with the other array utility wrappers.

Do NOT change any other code in the file. The entire change is one 3-line function addition.

Verify C function availability: `C.mlx_array_size` is declared in `mlx/c/mlx.h` (mlx-c v0.1.2). The return type is `size_t` (maps to `usize` in Zig). The comparison `== 0` produces a `bool`. This is correct.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && grep -n "arrayIsEmpty" src/mlx.zig/src/mlx.zig</automated>
  </verify>
  <done>`src/mlx.zig/src/mlx.zig` contains `pub fn arrayIsEmpty(arr: Array) bool` immediately after `arrayFree`</done>
</task>

<task type="auto">
  <name>Task 2: Verify arrayIsEmpty resolves linker errors in test build</name>
  <files></files>
  <action>
Run `zig build test 2>&1 | head -60` from the project root. The goal is to confirm the undefined-symbol linker error for `arrayIsEmpty` is gone.

Expected: `arrayIsEmpty` no longer appears in error output. The build may still fail for other reasons (MLA struct mismatch from GAP-10 is fixed in plan 03, other test failures are acceptable per D-05 as long as they are compile errors or assertion failures — not linker errors).

If `arrayIsEmpty` still appears as undefined: recheck that the function was added with `pub` visibility and that the file was saved correctly. Re-run the grep from Task 1 to confirm.

If a different undefined symbol appears that is clearly related to mlx bindings (i.e., another missing wrapper): note it in the SUMMARY for the team to address, but do NOT fix it in this plan — stay in scope.
  </action>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build test 2>&1 | grep -E "arrayIsEmpty|undefined symbol.*arrayIsEmpty" | head -10</automated>
  </verify>
  <done>Zero lines matching `arrayIsEmpty` appear in `zig build test` error output. The undefined symbol for `arrayIsEmpty` is resolved.</done>
</task>

</tasks>

<verification>
Run `zig build test 2>&1 | grep arrayIsEmpty`. Output must be empty. Then run `grep -c "arrayIsEmpty" src/test_integration.zig src/inference/dequantize.zig` to confirm the call sites were not removed — counts must be non-zero (per D-02).
</verification>

<success_criteria>
1. `src/mlx.zig/src/mlx.zig` contains `pub fn arrayIsEmpty(arr: Array) bool` wrapping `C.mlx_array_size(arr) == 0`
2. `zig build test` produces no linker error mentioning `arrayIsEmpty`
3. All call sites in `src/test_integration.zig` and `src/inference/dequantize.zig` remain unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/16-build-gap-closure/16-02-SUMMARY.md` with:
- Line number where arrayIsEmpty was inserted
- Output of `zig build test 2>&1 | head -40`
- Confirmation that call sites in test_integration.zig and dequantize.zig are unchanged
</output>
