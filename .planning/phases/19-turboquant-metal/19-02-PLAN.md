---
phase: 19-turboquant-metal
plan: 02
type: execute
wave: 1
depends_on:
  - 19-01
files_modified:
  - src/turboquant_test.zig
  - build.zig (verify test target exists)
autonomous: true
requirements:
  - TURBO-02
must_haves:
  truths:
    - "zig build test runs turboquant tests successfully"
    - "Compression roundtrip produces valid results"
    - "Compression ratio achieves at least 4x reduction"
  artifacts:
    - path: "src/turboquant_test.zig"
      provides: "Integration test for turboquant library"
      contains: "test \"turboquant"
  key_links:
    - from: "turboquant_test.zig"
      to: "deps/turboquant/turboquant/src/turboquant.zig"
      via: "@import(\"turboquant\")"
      pattern: "const turboquant = @import"
---

## Objective

Verify the botirk38/turboquant library works correctly through comprehensive tests.

**Purpose:** Ensure the TurboQuant compression library functions as expected before integrating into the KV cache system.

**Output:** Passing tests that verify compression/decompression roundtrip and compression ratios.

## Context

@.planning/phases/19-turboquant-metal/19-01-PLAN.md
@src/turboquant_test.zig
@deps/turboquant/turboquant/src/turboquant.zig

The turboquant library is already wired into build.zig as a module import:
```zig
exe.root_module.addImport("turboquant", turboquant_mod);
```

And there's a test file at src/turboquant_test.zig that imports it.

## Tasks

<task type="auto">
  <name>Task 1: Run existing turboquant tests</name>
  <read_first>
    - src/turboquant_test.zig
  </read_first>
  <files></files>
  <action>
    Run the turboquant-specific test target to verify the library works:
    1. Run zig build test with the turboquant_test filter
    2. Verify all tests pass
    3. Check compression roundtrip works
    4. Verify compression ratio meets expectations (4x+ reduction)
    
    The test should exercise:
    - Engine initialization
    - Compression roundtrip
    - Compression ratio validation
    - Dimension handling
  </action>
  <acceptance_criteria>
    - turboquant_test target runs without errors
    - All tests in the test file pass
    - Compression ratio test shows >= 4x reduction
    - No memory leaks (tests use testing.allocator)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build test --filter turboquant 2>&1 | tail -30</automated>
  </verify>
  <done>turboquant library tests pass, compression verified</done>
</task>

<task type="auto">
  <name>Task 2: Run turboquant_engine tests</name>
  <read_first>
    - src/compression/turboquant_engine.zig
  </read_first>
  <files></files>
  <action>
    Run tests for the turboquant_engine wrapper:
    1. Test engine caching (same dimension returns cached engine)
    2. Test compression/decompression roundtrip
    3. Test layer compression (K + V together)
    4. Verify refcount management
    
    These tests validate the engine wrapper around the raw turboquant library.
  </action>
  <acceptance_criteria>
    - Engine caching tests pass
    - Compression/decompression roundtrip works
    - Layer compression produces valid results
    - No memory leaks or crashes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build test --filter turboquant_engine 2>&1 | tail -20</automated>
  </verify>
  <done>turboquant_engine wrapper tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Run kv_compressor tests with TurboQuant</name>
  <read_first>
    - src/compression/kv_compressor.zig
  </read_first>
  <files></files>
  <action>
    Run the KvCompressor tests specifically for TurboQuant mode:
    1. Test TurboQuant compressor initialization
    2. Test compression returns correct metadata
    3. Test adaptive layer logic (first/last N layers uncompressed)
    4. Test compression ratio reporting
    
    These tests verify the high-level compressor interface works with TurboQuant backend.
  </action>
  <acceptance_criteria>
    - KvCompressor initializes with TurboQuant type
    - isEnabled() returns true for TurboQuant config
    - getCompressionRatio() returns ~5.5x
    - Adaptive layer compression logic works correctly
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build test --filter KvCompressor 2>&1 | tail -30</automated>
  </verify>
  <done>KvCompressor with TurboQuant backend passes all tests</done>
</task>

## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-19-03 | Denial of Service | Test failures | mitigate | Tests use testing.allocator which detects leaks |
| T-19-04 | Data Integrity | Compression roundtrip | mitigate | Tests verify decoded data matches original |

## Verification

- [ ] zig build test --filter turboquant passes
- [ ] zig build test --filter turboquant_engine passes
- [ ] zig build test --filter KvCompressor passes
- [ ] Compression ratio >= 4x verified in test output

## Success Criteria

1. All turboquant library tests pass
2. Engine wrapper tests pass
3. KvCompressor integration tests pass
4. Compression achieves at least 4x reduction
5. No memory leaks detected

## Output

After completion, create `.planning/phases/19-turboquant-metal/19-02-SUMMARY.md`
