---
phase: 19-turboquant-metal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/compression/mod.zig
  - src/compression/turboquant_stub.zig
autonomous: true
requirements:
  - TURBO-01
must_haves:
  truths:
    - "mod.zig exports real TurboQuant implementation instead of stub"
    - "Stub file is deleted or clearly marked deprecated"
    - "Build succeeds with no references to turboquant_stub"
  artifacts:
    - path: "src/compression/mod.zig"
      provides: "Clean module exports"
      exports: ["KvCompressor", "CompressionType", "CompressionConfig", "CompressionResult"]
  key_links:
    - from: "src/compression/mod.zig"
      to: "src/compression/kv_compressor.zig"
      via: "@import"
      pattern: "pub const.*= @import.*kv_compressor"
---

## Objective

Replace the stub TurboQuant export with the real implementation from kv_compressor.zig and clean up the module exports.

**Purpose:** Unblock the real TurboQuant integration by removing the indirection through the stub file.

**Output:** Clean module exports that use the real turboquant library via kv_compressor.zig

## Context

@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/compression/mod.zig
@src/compression/kv_compressor.zig
@src/compression/turboquant_engine.zig
@src/compression/turboquant_stub.zig

The build.zig already imports the turboquant library:
```zig
const turboquant_mod = b.createModule(.{
    .root_source_file = b.path("deps/turboquant/turboquant/src/turboquant.zig"),
});
exe.root_module.addImport("turboquant", turboquant_mod);
```

The kv_compressor.zig already imports turboquant_engine which uses the real library.

## Tasks

<task type="auto">
  <name>Task 1: Update mod.zig exports</name>
  <read_first>
    - src/compression/mod.zig (current state)
    - src/compression/kv_compressor.zig (available exports)
  </read_first>
  <files>src/compression/mod.zig</files>
  <action>
    Remove line 15 that exports TurboQuantCompressor from turboquant_stub.zig.
    Update the module doc comment to remove "stubbed" language since this is now real.
    The exports should be:
    - KvCompressor (the main interface)
    - CompressionType
    - CompressionConfig  
    - CompressionResult
    
    Do NOT export TurboQuantCompressor - it's an internal implementation detail.
    The KvCompressor uses TurboQuantEngine internally when CompressionType.TurboQuant is selected.
  </action>
  <acceptance_criteria>
    - mod.zig no longer contains "turboquant_stub"
    - mod.zig does not export TurboQuantCompressor
    - Module comment updated to reflect real implementation
    - All existing exports (KvCompressor, CompressionType, CompressionConfig, CompressionResult) remain
  </acceptance_criteria>
  <verify>
    <automated>grep -v "turboquant_stub" src/compression/mod.zig && grep -v "TurboQuantCompressor" src/compression/mod.zig</automated>
  </verify>
  <done>mod.zig exports only real implementation, no stub references</done>
</task>

<task type="auto">
  <name>Task 2: Mark stub file as deprecated</name>
  <read_first>
    - src/compression/turboquant_stub.zig (current state)
  </read_first>
  <files>src/compression/turboquant_stub.zig</files>
  <action>
    Add a prominent deprecation notice at the top of turboquant_stub.zig:
    - Mark the entire file as DEPRECATED
    - Explain that real implementation is now in turboquant_engine.zig + kv_compressor.zig
    - Note that the file is kept for reference only
    - Update module doc comment to reflect this status
    
    This preserves git history while making it clear the file should not be used.
  </action>
  <acceptance_criteria>
    - File starts with "//! DEPRECATED" notice
    - Clear explanation of where real implementation lives
    - Comment explaining file is kept for reference only
  </acceptance_criteria>
  <verify>
    <automated>head -20 src/compression/turboquant_stub.zig | grep -i "deprecated"</automated>
  </verify>
  <done>Stub file clearly marked deprecated with reference to real implementation</done>
</task>

<task type="auto">
  <name>Task 3: Verify clean build</name>
  <read_first>
    - build.zig (to confirm turboquant import)
  </read_first>
  <files></files>
  <action>
    Run zig build to verify:
    1. No compile errors from mod.zig changes
    2. TurboQuant library links correctly
    3. All compression module tests pass
    
    Run both the library build and tests.
  </action>
  <acceptance_criteria>
    - zig build completes with exit code 0
    - No warnings about turboquant_stub
    - zig build test runs compression tests successfully
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/gleicon/code/zig/zlx && zig build 2>&1 | tail -5 && zig build test 2>&1 | grep -E "(PASS|FAIL|error)" | head -20</automated>
  </verify>
  <done>Build succeeds with no stub references, tests pass</done>
</task>

## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-19-01 | Information Disclosure | turboquant_stub.zig | accept | File marked deprecated; no sensitive data in stub |
| T-19-02 | Denial of Service | mod.zig exports | mitigate | Keep all working exports, only remove stub reference |

## Verification

- [ ] grep -r "turboquant_stub" src/ returns no matches (except in stub file itself)
- [ ] zig build completes without errors
- [ ] All compression module tests pass
- [ ] KvCompressor can be instantiated from external code

## Success Criteria

1. mod.zig exports only the real implementation (kv_compressor.zig)
2. No code references turboquant_stub except the stub file itself
3. Build succeeds and tests pass
4. External users of compression module see no API changes

## Output

After completion, create `.planning/phases/19-turboquant-metal/19-01-SUMMARY.md`
