---
phase: 11-deepseek-moe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "build.zig",
  "src/mlx_v4.zig",
  "src/c_v4.zig",
  "src/mlx_v4_test.zig"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "mlx-c v0.4.1 is downloaded and compiled"
    - "v0.4.x library is linked as libmlxc-v4.a (no naming conflict)"
    - "Zig bindings compile without errors"
    - "Test Metal kernel can be created via Fast Ops API"
    - "Existing models (Qwen, Llama) still load and run"
  artifacts:
    - path: "build.zig"
      provides: "Dual mlx-c dependency download and linkage"
      contains: "mlx-c-v4 download steps"
    - path: "src/mlx_v4.zig"
      provides: "FastMetalKernel wrapper for v0.4.x"
      min_lines: 100
    - path: "src/c_v4.zig"
      provides: "Separate C import for v0.4.x headers"
      contains: "mlx/c/fast.h include"
    - path: "src/mlx_v4_test.zig"
      provides: "Test kernel creation"
      contains: "test \"mlx-c v0.4.x can create Metal kernel\""
  key_links:
    - from: "build.zig"
      to: "libmlxc-v4.a"
      via: "cmake and make build steps"
      pattern: "curl.*mlx-c.*v0.4.1"
    - from: "src/mlx_v4.zig"
      to: "src/c_v4.zig"
      via: "@import(\"c_v4.zig\")"
      pattern: "const c = @import"
---

<objective>
Integrate mlx-c v0.4.x alongside existing v0.1.2 to enable Fast Custom Ops API for MoE/MLA Metal kernels.

Purpose: Unlock mlx-c v0.4.x's FastMetalKernel API for custom Metal operations needed by MLA and MoE routing layers. Keep v0.1.2 for stability while adding v0.4.x capabilities.

Output: Dual dependency build system with working v0.4.x bindings and verified Metal kernel creation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key context from STATE.md:
- Strategy: Option 1 — mlx-c v0.4.x upgrade (8-12h vs 20-30h for custom implementation)
- MLX.zig currently pins mlx-c v0.1.2
- API changes between v0.1.2 and v0.4.x expected
- Need to maintain backward compatibility with existing models

Architecture:
- Keep mlx-c v0.1.2 for existing MLX.zig operations (stable)
- Add mlx-c v0.4.1 as new dependency for Fast Custom Ops API
- Isolate v0.4.x usage to MoE/MLA custom kernels only
- Rename v0.4.x library to libmlxc-v4.a to avoid symbol conflicts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mlx-c v0.4.1 download to build.zig</name>
  <files>build.zig</files>
  <behavior>
    - Build downloads v0.4.1 when libmlxc-v4.a missing
    - Build renames libmlxc.a to libmlxc-v4.a after compile
    - Download uses curl to fetch v0.4.1 tarball
    - Build uses cmake + make to compile
  </behavior>
  <action>
    Modify build.zig to add parallel download step for mlx-c v0.4.x:
    
    1. After existing mlx-c v0.1.2 setup, add:
       - Define mlx_c_v4_path in cache root
       - Check if libmlxc-v4.a exists
       - If missing, download v0.4.1 tarball via curl
       - Extract and build with cmake + make
       - Rename libmlxc.a to libmlxc-v4.a to avoid conflicts
    
    2. The download command should:
       - Use curl -L to follow redirects
       - Pipe to tar xz for extraction
       - Place in .zig-cache/mlx-c-v4/
    
    3. Build steps:
       - mkdir -p for build directory
       - cmake .. with Release mode and PIC
       - make -j4 for parallel compilation
       - mv libmlxc.a libmlxc-v4.a
    
    Note: Use b.addSystemCommand for shell commands.
    Keep existing v0.1.2 logic intact.
  </action>
  <verify>
    <automated>ls .zig-cache/mlx-c-v4/build/libmlxc-v4.a 2>/dev/null && echo "v0.4.x library exists" || echo "NEED BUILD"</automated>
  </verify>
  <done>mlx-c v0.4.1 downloads and compiles; libmlxc-v4.a exists in cache</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Zig bindings for v0.4.x Fast Ops API</name>
  <files>src/mlx_v4.zig, src/c_v4.zig</files>
  <behavior>
    - FastMetalKernel.init creates kernel from Metal source
    - FastMetalKernel.apply executes kernel with grid/thread config
    - FastMetalKernel.deinit frees kernel resources
    - Uses separate c_v4.zig for C imports to avoid conflicts
  </behavior>
  <action>
    Create src/mlx_v4.zig with FastMetalKernel wrapper:
    
    1. Create src/c_v4.zig first:
       - Use @cImport with MLX_C_VERSION 0.4.1
       - Include mlx/c/fast.h for custom ops
       - Include mlx/c/vector.h, array.h, stream.h
    
    2. Create src/mlx_v4.zig:
       - Import c_v4.zig as separate namespace
       - Define FastMetalKernel struct with kernel handle and allocator
       - Implement init() that:
         * Takes allocator, name, input/output names, Metal source
         * Converts strings to C-compatible format
         * Calls mlx_fast_metal_kernel_new
         * Returns kernel or error
       - Implement deinit() that calls mlx_fast_metal_kernel_free
       - Implement apply() that:
         * Creates mlx_fast_metal_kernel_config
         * Sets grid and thread_group dimensions
         * Calls mlx_fast_metal_kernel_apply
         * Returns error if non-zero result
    
    3. Add helper function hasFastOps() to check if v0.4.x available
  </action>
  <verify>
    <automated>zig build 2>&1 | grep -i "error" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>mlx_v4.zig and c_v4.zig compile without errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update build.zig to link v0.4.x library</name>
  <files>build.zig</files>
  <behavior>
    - Build adds library path for libmlxc-v4.a
    - Build links mlxc-v4 system library
    - Build adds include path for v0.4.x headers
    - Build adds rpath for runtime discovery
    - Existing v0.1.2 linkage preserved
  </behavior>
  <action>
    Modify configureExecutable in build.zig:
    
    1. Add v0.4.x library paths:
       - mlx_c_v4_lib_path = cache/mlx-c-v4/build
       - mlx_c_v4_include = cache/mlx-c-v4/mlx/c
    
    2. Add to exe configuration:
       - exe.addLibraryPath for v0.4.x build dir
       - exe.linkSystemLibrary("mlxc-v4")
       - exe.addIncludePath for v0.4.x headers
       - exe.addRPath for v0.4.x library path
    
    3. Ensure v0.1.2 and v0.4.x don't conflict:
       - Different library names (libmlxc.a vs libmlxc-v4.a)
       - Different include paths
       - Separate C import files (c.zig vs c_v4.zig)
    
    4. Make v0.4.x optional:
       - Build succeeds even if v0.4.x download fails
       - hasFastOps() returns false if library missing
  </action>
  <verify>
    <automated>zig build 2>&1 | tail -5 | grep -q "error" && echo "FAIL" || echo "PASS"</automated>
  </verify>
  <done>Build succeeds and links both v0.1.2 and v0.4.x libraries</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create test kernel verification</name>
  <files>src/mlx_v4_test.zig</files>
  <behavior>
    - Test creates FastMetalKernel with simple shader
    - Test kernel multiplies input by 2.0
    - Test passes if kernel creation succeeds
    - Test skipped if v0.4.x not available
  </behavior>
  <action>
    Create src/mlx_v4_test.zig:
    
    1. Import mlx_v4 module
    2. Define test "mlx-c v0.4.x can create Metal kernel":
       - Create test kernel source:
         ```
         kernel void test_kernel(
             device float* out [[buffer(0)]],
             device const float* in [[buffer(1)]],
             uint tid [[thread_position_in_grid]]
         ) {
             out[tid] = in[tid] * 2.0;
         }
         ```
       - Initialize FastMetalKernel with:
         * name: "test_kernel"
         * input_names: ["input"]
         * output_names: ["output"]
         * source: embedded shader
       - Deinit kernel
       - Assert true if no errors
    
    3. Add test "mlx-c v0.4.x Fast Ops available":
       - Check hasFastOps() returns true
       - Skip test if false (build without v0.4.x)
  </action>
  <verify>
    <automated>zig build test -- src/mlx_v4_test.zig 2>&1 | grep -E "(PASS|FAIL|error)" | head -5</automated>
  </verify>
  <done>Test kernel creation succeeds; tests pass</done>
</task>

<task type="auto">
  <name>Task 5: Verify backward compatibility</name>
  <files>src/main.zig (indirect)</files>
  <behavior>
    - Existing Qwen model loads successfully
    - Existing Llama model loads successfully
    - Existing Phi model loads successfully
    - Server starts without errors
    - --help works
  </behavior>
  <action>
    Run integration verification:
    
    1. Build the project:
       ```bash
       zig build
       ```
    
    2. Verify binary runs:
       ```bash
       ./zig-out/bin/zlx --help
       ```
    
    3. Check existing models still work (if available):
       ```bash
       ./zig-out/bin/zlx --model qwen2.5-coder-1.5b --configure-opencode
       ```
    
    4. Verify no symbol conflicts:
       ```bash
       nm zig-out/bin/zlx | grep -E "(mlx_|mlxc)" | head -20
       ```
    
    Note: Full model testing requires models present. Basic run test is sufficient.
  </action>
  <verify>
    <automated>./zig-out/bin/zlx --help 2>&1 | grep -q "Usage:" && echo "BACKWARD_COMPAT_OK" || echo "FAIL"</automated>
  </verify>
  <done>Binary runs successfully; existing functionality preserved</done>
</task>

</tasks>

<verification>
## Phase 11-01 Verification Checklist

1. **Build System:**
   - [ ] `zig build` downloads and compiles v0.4.x automatically
   - [ ] `libmlxc-v4.a` exists in `.zig-cache/mlx-c-v4/build/`
   - [ ] Both v0.1.2 and v0.4.x libraries linked

2. **Code Quality:**
   - [ ] `src/mlx_v4.zig` compiles without errors
   - [ ] `src/c_v4.zig` has proper C imports
   - [ ] No duplicate symbol errors during link

3. **Functionality:**
   - [ ] `zig build test -- src/mlx_v4_test.zig` passes
   - [ ] FastMetalKernel can be created
   - [ ] Binary runs with `--help`
   - [ ] Existing models still load (if available)

4. **Risk Mitigation:**
   - [ ] Symbol conflicts avoided via library renaming
   - [ ] Build graceful if v0.4.x download fails
   - [ ] Backward compatibility verified
</verification>

<success_criteria>
- mlx-c v0.4.1 downloaded to `.zig-cache/mlx-c-v4/`
- v0.4.x library compiled as `libmlxc-v4.a`
- Zig bindings in `src/mlx_v4.zig` compile successfully
- Test Metal kernel can be created via Fast Ops API
- Existing models (Qwen, Llama) continue to work
- Both v0.1.2 and v0.4.x linked in final binary without conflicts
</success_criteria>

<output>
After completion, create `.planning/phases/11-deepseek-moe/11-01-SUMMARY.md`
</output>
